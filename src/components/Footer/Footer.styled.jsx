import styled from 'styled-components';
import { footerColor } from '../../styles/colors';

export const FooterStyled = styled.footer`
  padding-top: 24px;
  text-align: center;
  height: 146px;
  background-color: ${footerColor};
  font-size: 14px;
  font-weight: 500;
`;
